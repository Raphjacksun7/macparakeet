# Browser adapter implementation ownership
Worktree /Users/dmoon/code/macparakeet-jev-voice-control. Build explicit authorized Chromium MV3 extension + native messaging host + Swift adapter bridge for full feature. Read AGENTS and Fable design review. AX baseline proof happens independently; extension still requested full scope. You own integrations/voice-control-browser/, Sources/VoiceControlBrowserHost/, Sources/MacParakeetCore/Services/VoiceControl/VoiceControlBrowser*.swift, browser tests and Package.swift host-target edits. Coordinate interface with /root/core_implementation immediately. You are not alone; do not revert other edits. No commits, builds only with parent coordination. Parent owns native adapter/docs; UI worker owns app wiring, tell it integration API.

Settled transport: persistent native messaging host stdio framing to private app Unix socket, or equivalently secure demonstrably simpler local channel if justified to parent first. Explicit chosen tab/profile/document, never first-tab fallback. Host exact caller extension-origin allowlist, user-level installation action documented, no admin/relaunch/debug-port. Socket 0700 parent/0600 socket, authenticated per-connection session, bounded frames/timeouts. No page postMessage commands, no arbitrary executable model source/selectors. Same Swift typed capabilities as native adapter. DOM snapshots retain actual nodes; execution rechecks attached, live semantic identity, enabled/visible/occlusion, value revision. Secure inputs excluded before value reads; minimize visible page text. Fresh document IDs on navigation; disconnected/revoked task drops effects. Remote dispatch stop cannot unsend already authorized effect; document boundary, expire quickly and ACK receipts, no retry after unknown. Browser command scope isolated to explicitly authorized tab. Include popup UI for connect/disconnect authorized tab and clear consent; no API key in extension.

Tests: JS tests for DOM semantics with Playwright (existing tool if installed), framed protocol/Swift tests if feasible, install scripts static validation. Do not claim physical browser extension qualification without running. Document README setup/build, capability limitations (frames/closed shadows etc) and protocol. Send status and integration contract quickly. If using claude CLI Sonnet5 high internally, use durable bounded briefs and no uncontrolled overlapping writes.
