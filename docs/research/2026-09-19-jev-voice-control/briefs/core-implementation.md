# Core implementation ownership
Worktree /Users/dmoon/code/macparakeet-jev-voice-control only. Read AGENTS.md and implementation-fable-review.md. Build typed Voice Control core with Jev HTTP integration, bounded goal runner and focused tests. Own Sources/MacParakeetCore/Services/VoiceControl/ EXCEPT NativeVoiceControlAdapter.swift, VoiceControlSpeechSession.swift, VoiceControlBrowser* (others own those). Own Tests/MacParakeetTests/VoiceControl/ core tests except native/speech/browser files. You are not alone; do not revert others. Do not modify UI or existing capture code. Do not commit (parent checkpoints).

Settled: full feature, generic goal loop plus direct commands, raw committed transcripts, cloud explicit consent, pinned jev-1.13.0, observed target IDs only, source-span text values, optional generation dependency for rewrite only. Typed actions press,setValue,insertText,select,scroll,key,activateApp; capability-filter candidates. Native/browser adapters implement shared Sendable protocol observe() async throws -> VoiceControlSnapshot, execute(action:snapshot:authority:) async throws -> VoiceControlReceipt. You may improve exact parameter names but establish and send parent interface file IMMEDIATELY before runner implementation. ActionAuthority must be synchronous revocable Sendable token checked immediately before EACH effect; local Stop must revoke without waiting on actor doing IO. Snapshot identities and opaque target references cannot be replayed across revisions. Receipt verified/unknown/failed and no blind replay. Support confirmation, clarification, correction/resume/cancel, budget12 actions/30 requests/60s/2 no-progress. Unknown GUI presses should require confirmation unless adapter marks a proven navigation capability; no model risk decision substitutes local policy. Auth/error responses must never echo secrets/context. No request logging.

Live wire documentation /tmp/jev-api.md and skill in original checkout .agents/skills/typesafe-ai/SKILL.md. Do not read keys. Read references only as needed. Strict response validation membership and probabilities. Expose injectable URLSession/transport tests. Keychain credential/consent storage can be in own files. Keep simple maintainable APIs; defaults injectable for testing. Core runner must be usable by app UI and offline/live qualification harness. For goal Done use observed postconditions, distinguish inferred completion if no independent proof. Establish contract first, then implement, then focused tests when parent allows build lock. Do NOT run full suite. Report exact files, tests, gaps honestly. Parent owns build coordination; ask before invoking Swift build/test.
